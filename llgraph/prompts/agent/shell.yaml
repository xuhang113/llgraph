# 并入 Actions / Using tools；保留短余量供兼容

run_shell: |
  `run_shell_command` 仅用于必须走 shell 的工作（git、构建、测试、docker）。禁止用 shell 列目录/搜索/cat 做发现。

paths_and_spill: |
  路径相对工作区。若工具结果提示内容已落盘/归档，按需用 `read_file` 行段读取；勿把预览当成全文。
