---
description: Cloud Agent 开工/收工与 changelog
alwaysApply: true
---

# Cloud Agent

- 开工：`AGENTS.md` → `changelog/` 最近 3 条 → 下手文件
- 人手轮：只做点名需求
- 定时轮：按 `AGENTS.md`「Cloud Agent 排队」。模型入口 2026-09-17 完成，ACP 编辑器入口 2026-09-18 落地，写入授权弹窗 2026-09-19 落地（编辑器里已能真改代码），`session/load` 续聊 2026-09-20 落地（重启后接得回旧会话），`fs/*` 反向请求 2026-09-21 落地（读写未保存缓冲区），`tool_call` 三段状态 2026-09-22 落地（工具跑之前就看到）；当前优先工具失败态与 `locations`，VS Code 扩展与终端 TUI 后置。不要两件一起开。不要做 PyPI / brew
- 先看 changelog 再动手，不要推倒重来
- 收工必须新增 changelog；没有高价值切口只写下一步
- 禁止提交密钥、`.env`、网关令牌
- 改完相关测试要通过，CLI 入口不要弄坏
