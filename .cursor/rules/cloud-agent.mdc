---
description: Cloud Agent 开工/收工与 changelog
alwaysApply: true
---

# Cloud Agent

- 开工：`AGENTS.md` → `changelog/` 最近 3 条 → 下手文件
- 人手轮：只做点名需求
- 定时轮：按 `AGENTS.md`「Cloud Agent 排队」。模型入口 2026-09-17 完成，ACP 编辑器入口 2026-09-18 落地；当前优先把「编辑器里干活」做到能真改代码（授权弹窗、续聊），VS Code 扩展与终端 TUI 后置。不要两件一起开。不要做 PyPI / brew
- 先看 changelog 再动手，不要推倒重来
- 收工必须新增 changelog；没有高价值切口只写下一步
- 禁止提交密钥、`.env`、网关令牌
- 改完相关测试要通过，CLI 入口不要弄坏
