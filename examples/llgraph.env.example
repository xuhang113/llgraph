# llgraph 模型入口配置
# 安装: cp llgraph.env.example ~/.config/llgraph/llgraph.env 后编辑
#
# 四条路任选一条（占位符照抄没用，填自己的）：
#   1. OpenAI 兼容网关（默认；两个变量齐就走这条）
#   2. Anthropic / OpenAI / Gemini 官方 Key
#   3. 本地 Ollama（不需要 Key）
#   4. 显式指定 LLGRAPH_PROVIDER，压过上面的自动判断
#
# 自动判断顺序：LLGRAPH_PROVIDER > 网关凭据 > LLGRAPH_MODEL 指向且配了 Key 的那家
#              > 唯一配了 Key 的官方厂商 > 本机在听的 Ollama
# 显式指定: gateway | anthropic | openai | gemini | ollama
# LLGRAPH_PROVIDER=anthropic

# --- 1. OpenAI 兼容网关（与 Claude CLI / ANTHROPIC_* 无关）---
LLGRAPH_API_BASE_URL=https://your-ai-gateway.example
LLGRAPH_API_KEY=your-llgraph-api-token
LLGRAPH_MODEL=claude-opus-4-6

# --- 2. 官方 Key（LLGRAPH_ 前缀优先；没写则回退官方变量名 ANTHROPIC_API_KEY /
#        OPENAI_API_KEY / GEMINI_API_KEY / GOOGLE_API_KEY）---
# 需要 pip install 'llgraph[openai]' / 'llgraph[gemini]'（anthropic 已内置）
# LLGRAPH_ANTHROPIC_API_KEY=
# LLGRAPH_OPENAI_API_KEY=
# LLGRAPH_GEMINI_API_KEY=
# 只认 LLGRAPH_ 前缀的自建地址（官方 *_BASE_URL 常被别的工具指到代理上，不自动继承）
# LLGRAPH_ANTHROPIC_BASE_URL=
# LLGRAPH_OPENAI_BASE_URL=

# --- 3. 本地 Ollama（pip install 'llgraph[ollama]'；先 ollama serve + ollama pull）---
# 默认地址 http://127.0.0.1:11434，端口在听就能被自动认出来
# LLGRAPH_OLLAMA_BASE_URL=http://127.0.0.1:11434
# 未设 LLGRAPH_MODEL 时各家默认模型: anthropic=claude-sonnet-4-5 openai=gpt-4.1
# gemini=gemini-2.5-flash ollama=qwen3:8b（Ollama 请挑支持 tools 的模型）
# LLGRAPH_MODEL=qwen3:8b

# Tavily Web 搜索（/web on 启用；免费 1000 credits/月，见 https://docs.tavily.com/documentation/api-credits）
TAVILY_API_KEY=tvly-your-api-key
