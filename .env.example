# llgraph 专用 API 凭据（与 Claude CLI / ANTHROPIC_* 无关）
# 复制到 ~/.config/llgraph/llgraph.env 后填写

# OpenAI 兼容网关根地址（不含 /v1 后缀）
LLGRAPH_API_BASE_URL=https://your-ai-gateway.example

# 在网关控制台创建的 API 令牌
LLGRAPH_API_KEY=your-llgraph-api-token

# 默认对话模型
LLGRAPH_MODEL=claude-opus-4-6
